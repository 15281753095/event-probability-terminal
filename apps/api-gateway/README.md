# API Gateway

Minimal Fastify gateway shell.

## Current endpoint

- `GET /healthz`: local process health only.

## Boundary

No external market request is implemented here. Future external calls must go through source-specific adapters after `docs/source_registry.md` and `docs/api/*.md` are updated.


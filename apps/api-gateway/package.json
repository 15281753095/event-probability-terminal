{
  "name": "@ept/api-gateway",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"api-gateway lint not configured yet\"",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@ept/market-ingestor": "workspace:*",
    "@ept/shared-types": "workspace:*",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
